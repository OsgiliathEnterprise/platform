[general]
config_version = 2

[slapd]
root_password = {{ ds_admin_password }}

[backend-userroot]
sample_entries = no
{% set domains = company_domain.split('.') %}
{% set separator = joiner(',') %}
suffix = {% for dc in domains %}{{ separator() }}dc={{ dc }}{% endfor %}